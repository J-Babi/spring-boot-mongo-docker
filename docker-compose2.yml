version: '3.1'
services:
  springboot:
    image: J-Babi/spring-boot-mongo-docker
    restart: always # This will be ignored if we deploy in docker swarm
    ports:
      - 800:8080
    networks:
      -boa
networks:
  boa: 
